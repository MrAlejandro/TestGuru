<tr>
  <td scope="row"><%= test.title %></td>
  <td><%= test_level(test.level) %></td>
  <td><%= test.category.title %></td>
  <td><%= test.time_limit > 0 ? test.time_limit : t(".no_limit") %></td>
  <td><%= button_to t(".start"), start_test_path(test), class: "btn btn-primary" %></td>
</tr>
