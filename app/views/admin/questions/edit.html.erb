<h1><%= question_title(@test, @question) %></h1>

<%= form_with model: [:admin, @question], local: true, method: :put do |form| %>
  <% render "form_fields", form: form %>
<% end %>

<%= link_to "Back", admin_question_path(@question) %>
